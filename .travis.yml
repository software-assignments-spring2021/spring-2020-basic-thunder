os: osx

sudo: true

language: node_js

node_js:
  - node

addons:
  ssh_known_hosts: $PRODUCTION_SERVER
  apt:
    packages:
      - expect

notifications:
  email:
    recipients:
      - sylvia.ji@nyu.edu
      - jl8456@nyu.edu
      - cem663@nyu.edu
    on_success: never
    on_failure: always

before_script:
  - cd back-end
#  - npm install

script:
  # - npm test
  echo DONE

#deploy:
after_success:
  - cd ..
  - chmod +x deploy.expect
  - expect deploy.expect
